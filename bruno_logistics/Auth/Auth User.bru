meta {
  name: Auth User
  type: http
  seq: 5
}

get {
  url: {{api_url}}/auth/user
  body: json
  auth: none
}

body:json {
  {
    "email":"m1@mail.pt",
    "password":"123"
  }
}

tests {
  var token = res.getBody();
  bru.setEnvVar("token",token);
}
